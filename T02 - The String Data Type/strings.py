# Creating a variable called hero with a 'decorated' string as a value:
hero = "$$$Superman$$$"
# Printing the value of the variable without its decoration, using the strip method:
print(hero.strip("$"))